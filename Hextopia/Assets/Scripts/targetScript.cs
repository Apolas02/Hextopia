using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class targetScript : MonoBehaviour
{
    //RaycastHit hit;
    //Vector3 movePoint;
    //clickTarget clicked;
    //GameObject target;
    ////GameObject newTarget;




    //// Start is called before the first frame update
    //void Start()
    //{
    //    target = clicked.clickedTarget.gameObject.transform.GetChild(0).gameObject;
    //    //newTarget = Instantiate(target, transform.position, transform.rotation);
    //    Ray ray = Camera.main.ScreenPointToRay(Input.mousePosition);

    //    if (Physics.Raycast(ray, out hit, 50000.0f, (0 << 8)))
    //    {
    //        //newTarget.transform.position = hit.point;
    //    }
    //}

    //// Update is called once per frame
    //void Update()
    //{
        
    //    Ray ray = Camera.main.ScreenPointToRay(Input.mousePosition);

    //    if (Physics.Raycast(ray, out hit, 50.0f))
    //    {
    //        //newTarget.transform.position = hit.point;
    //    }

    //    if (Input.GetMouseButtonDown(0))
    //    {
    //        //target.transform.position = newTarget.transform.position;
    //        //target.transform.rotation = newTarget.transform.rotation;
    //        //Destroy(newTarget);
    //    }


    //    if (Input.GetKey(KeyCode.Escape) || Input.GetKey(KeyCode.Mouse1))
    //    {
    //        Destroy(gameObject);
    //    }
    //}
}
